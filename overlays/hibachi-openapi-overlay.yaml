# generated: '2026-07-19'
# method: generated
# source: openapi/hibachi-openapi.yml
overlay: 1.0.0
info:
  title: API Evangelist enhancements for Hibachi API
  version: 1.0.0
extends: openapi/hibachi-openapi.yml
actions:
- target: $.info
  update:
    x-apievangelist-enriched: true
    x-apievangelist-source: https://api-doc.hibachi.xyz/
    x-api-category: defi-perpetuals-exchange
- target: $.info
  update:
    x-agent-notes: >-
      Public /market and /exchange endpoints need no auth. All other endpoints require an
      Authorization API key; order/withdraw/transfer additionally require a signed payload
      (ECDSA or HMAC). Quantities and prices are strings. Discover tradable symbols from
      /market/exchange-info before trading.
- target: $.tags
  update:
    - name: Capital
      x-agentic-consequence: physical
      description: >-
        Capital movement (withdraw/transfer) has irreversible financial consequences and requires
        a signed payload.
